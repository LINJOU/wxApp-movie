<view class="star">
  <text class="star-item {{item}}" wx:for="{{itemClasses}}" wx:key="index" style="width: {{size}}rpx; height: {{size}}rpx; background-size: {{size}}rpx {{size}}rpx;"></text>
</view>